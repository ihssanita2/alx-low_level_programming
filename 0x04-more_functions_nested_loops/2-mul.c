#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: the first num to multiplie.
 * @b: the second num to multiplie.
 * Return: 1 if true 0 otherwise.
 */
int mul(int a, int b)
{
	int mul;

	return (mul = a * b);
}

#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @x: first num.
 * @y: second num.
 * Return: the sum of tow integers.
 */
int add(int x, int y)
{
	int s;

	s = x + y;
	return (s);
}
